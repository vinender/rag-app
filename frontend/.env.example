# Base URL of the RAG backend. No trailing slash.
# Local dev: the backend default port is 3000 (or whatever PORT you set).
NEXT_PUBLIC_API_URL=http://localhost:3000

# Production: your deployed backend, e.g.
# NEXT_PUBLIC_API_URL=https://rag-backend.onrender.com
