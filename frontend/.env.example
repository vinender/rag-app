# The browser calls the Next.js /api/* routes, which proxy to the FastAPI
# backend using this SERVER-side var (not exposed to the client).
# Local dev: defaults to http://localhost:8000 if unset.
# Production (Vercel): set to your deployed FastAPI URL.
BACKEND_URL=https://rag-backend.onrender.com
